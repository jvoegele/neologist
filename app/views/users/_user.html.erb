<div class="well sidebar-nav">
  <h3 class="text-info"><%=user.full_name%></h3>
  <h4 class="text-info">@<%=user.username%></h4>
  <ul>
    <li><%= render_with_locals('users/quip_stats', user: user) %></li>
    <li><%= render_with_locals('users/following_stats', user: user) %></li>
    <li><%= render_with_locals('users/followers_stats', user: user) %></li>
  </ul>

  <% if current_user %>
    <%= render partial: 'users/follow_form', locals: {user: user} %>
    <%= link_to "Compose new Quip", quip_path, class: "btn btn-primary"%>
  <% end %>
</div>
