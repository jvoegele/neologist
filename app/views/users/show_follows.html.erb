<div class="row">
  <div class="span8">
    <% if @users.any? %>
      <ul class="users">
        <%= render partial: 'user_brief', collection: @users, as: :user %>
      </ul>
      <%= will_paginate %>
    <% end %>
  </div>
</div>
